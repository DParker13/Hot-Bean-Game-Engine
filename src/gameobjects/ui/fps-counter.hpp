#pragma once

#include "text.hpp"

namespace GameObjects {
    namespace UI {
        class FPSCounter : public Text {
        public:
            FPSCounter(Core::CoreManager* coreManager);

            
        };
    }
}