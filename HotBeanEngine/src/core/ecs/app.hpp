#pragma once

// Core includes
#include <SDL.h>
#include <SDL_ttf.h>
#include <SDL_mixer.h>
#include <SDL_image.h>

namespace Core {
    namespace Application {
        // Forward declaration
        class App;
    }
}