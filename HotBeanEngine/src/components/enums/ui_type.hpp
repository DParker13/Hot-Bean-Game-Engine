#pragma once

namespace Components {
    namespace Enums {
        enum class UIType {
            Empty,
            Text,
            Image,
            Button,
            Slider,
            Dropdown,
            Checkbox,
            Radio,
        };
    }
}