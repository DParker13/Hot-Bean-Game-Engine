#pragma once

namespace Components {
    namespace Enums {
        enum class UIType {
            Text,
            Image,
            Button,
            Slider,
            Dropdown,
            Checkbox,
            Radio,
        };
    }
}